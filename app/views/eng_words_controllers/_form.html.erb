<%= form_for(eng_words_controller) do |f| %>
  <% if eng_words_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eng_words_controller.errors.count, "error") %> prohibited this eng_words_controller from being saved:</h2>

      <ul>
      <% eng_words_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
