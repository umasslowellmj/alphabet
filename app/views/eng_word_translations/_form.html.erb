<%= form_for(eng_word_translation) do |f| %>
  <% if eng_word_translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eng_word_translation.errors.count, "error") %> prohibited this eng_word_translation from being saved:</h2>

      <ul>
      <% eng_word_translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
